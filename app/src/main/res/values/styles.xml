<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->

    </style>

    <style name="AppTheme.Dark" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryColorDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="android:background">@color/primaryColor</item>
    </style>

    <style name="LinearProgress" parent="Material.Drawable.LinearProgress">
        <item name="lpd_strokeColor">@color/flatRed</item>
        <item name="lpd_strokeSecondaryColor">@color/pastelRed</item>
        <item name="lpd_travelDuration">2000</item>
    </style>

</resources>
